<div align="center">

# Sessions

<sub>Concepts</sub>

<br>

[Documentation home](../README.md)&nbsp;&nbsp;•&nbsp;&nbsp;[Repository README](../../README.md)

</div>

---

## At a glance

- [What a session is](#what-a-session-is)
- [Sessions and identity](#sessions-and-identity)
- [Boundaries](#boundaries)
- [Reports](#reports)

---

Sessions are a future observability and organization feature: a way to group the executions
of one workflow into a single reportable unit. Like everything in gmlcache they are
**single-user** — there is no multi-user namespace above them.

## What a session is

A session is a **workflow boundary** — an append-only log of invocation events
`(session_id, timestamp, fingerprint)` recorded as a workflow runs. It captures *that a call
happened again*, which a tag cannot: a tag lives on a cache entry (one key, one tag, N hits)
and cannot count the two times the same cached call was invoked inside a workflow, or show
that the second cost nothing.

Sessions are generated by gmlcache. A session needs only a **generated session id** — it does
**not** require any token, account, or namespace.

## Sessions and identity

A session is **metadata, never part of execution identity** (the fingerprint). The same input
under different sessions is still one cache entry. Sessions observe and group executions; they
never change what counts as "the same call."

> Design detail (open): counting in-session cache *hits* means distinguishing an **invocation
> event** (every call, hit or miss) from an **execution event** (only when a real model call
> happened) — a miss is both, a hit is invocation-only. This is to be ruled as part of the
> session design before sessions are built.

## Boundaries

Typical session boundaries:

- one CI pipeline run,
- one workflow engine run,
- one evaluation batch,
- one feature-generation pass,
- one scripted experiment.

## Reports

A session report can show executions, cache hits, records, non-cacheable calls, adapter/model
breakdown, token usage, client-reported cost, and unknown-cost executions — all for a single
user's own workflow.

---

<div align="center">

<sub>[Documentation home](../README.md)&nbsp;&nbsp;•&nbsp;&nbsp;[Repository README](../../README.md)</sub>

</div>
